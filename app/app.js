(function () {
    'use strict';

    angular
        .module('app.core', [
            'ui.router',
            'registerStateApp',
            'app.pages'
        ]).config(config);

        config.$inject = ['dependency'];

        /* @ngInject */
        function config (dependency) {
            code
        }

})();